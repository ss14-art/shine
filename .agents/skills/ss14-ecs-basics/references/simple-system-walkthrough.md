# Simple System Walkthrough

A minimal SS14 feature usually has:

- a component with data;
- a system with event subscriptions and public API;
- prototypes that add the component;
- FTL for visible text;
- tests or manual validation.

For Shine, place new files under `_sh` and mark any non-`_sh` integration edit.
