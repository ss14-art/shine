# Predicted BUI Patterns

- Use predicted messages for client input when gameplay should respond immediately.
- Prefer component state over duplicate BUI state.
- Keep server validation authoritative.
- Update UI idempotently during prediction replay.
