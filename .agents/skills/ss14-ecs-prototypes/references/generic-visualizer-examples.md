# Generic Visualizer Examples

Use `GenericVisualizer` to map appearance data to sprite-layer state or visibility.

Keep:

- appearance keys serializable;
- layer names stable;
- component state and appearance state updated together.
