# UI Controller Lifetime

Check:

- subscriptions are cleaned up;
- windows handle close/disconnect;
- state refreshes after network updates;
- client-only dependencies stay client-only;
- no gameplay authority is placed in UI code.
