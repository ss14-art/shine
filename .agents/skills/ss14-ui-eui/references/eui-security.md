# EUI Security

EUI actions are often admin/server oriented.

Validate:

- admin permissions;
- session identity;
- target existence;
- stale UI state;
- whether the action is still allowed.

Never rely on client-side button visibility for security.
