# EUI Vs BUI

Use BUI for entity-bound gameplay UI.

Use EUI for:

- admin tools;
- server/service UI;
- workflows not tied to a specific in-world entity.

If the UI controls an entity, BUI is usually the first candidate.
