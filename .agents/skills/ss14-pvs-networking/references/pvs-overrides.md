# PVS Overrides

Use overrides when a client must see an entity outside normal PVS.

Rules:

- prefer session-scoped overrides;
- remove overrides promptly;
- document why normal PVS is insufficient;
- avoid global overrides unless truly global.
