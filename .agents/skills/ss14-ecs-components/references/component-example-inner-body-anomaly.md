# Component Example Notes

When looking for local examples, prefer recent shared components that:

- use `partial`;
- use typed DataFields;
- use access restrictions;
- use AutoNetworkedField only for data clients need;
- leave mutation to systems.

Do not copy older components that mix logic or legacy networking unless maintaining that subsystem.
