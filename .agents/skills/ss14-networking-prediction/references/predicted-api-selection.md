# Predicted API Selection

Use predicted APIs for:

- popup feedback;
- audio feedback;
- entity spawn/delete;
- BUI input messages.

Use server-only APIs when:

- the client cannot know enough state;
- hidden data is involved;
- simulation is not predictable.
