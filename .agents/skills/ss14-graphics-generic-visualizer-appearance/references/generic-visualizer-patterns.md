# GenericVisualizer Patterns

Use GenericVisualizer when prototype YAML can map appearance data to sprite-layer changes.

Good for:

- simple booleans;
- enum-like states;
- layer visibility;
- state changes.

Avoid when:

- behavior needs complex client logic;
- visual state is not represented by appearance data;
- shader/overlay behavior is required.
