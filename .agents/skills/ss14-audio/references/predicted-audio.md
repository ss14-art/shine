# Predicted Audio

Use `PlayPredicted` for immediate feedback in predicted shared paths.

Avoid:

- `PlayPvs` in predicted interaction code;
- audio in prediction replay without guards;
- hardcoded paths in systems.

Test with fake lag when audio is tied to player input.
