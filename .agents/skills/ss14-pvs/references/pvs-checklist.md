# PVS Checklist

- Does the client need this entity/data?
- What happens when it leaves PVS?
- Is hidden data being networked?
- Is a PVS override temporary and removed?
- Does predicted code handle nullspace/detach behavior?
- Was it tested at distance or with two clients?
