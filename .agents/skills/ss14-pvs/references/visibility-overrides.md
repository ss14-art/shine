# Visibility Overrides

Use overrides only when a client genuinely needs data outside normal PVS.

Rules:

- scope to the player/session when possible;
- remove when no longer needed;
- document why normal PVS is insufficient;
- avoid global overrides for convenience.
