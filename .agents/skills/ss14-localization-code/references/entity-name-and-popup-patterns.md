# Entity Name And Popup Patterns

## Popup

- Use `PopupPredicted` in predicted paths.
- Use `PopupClient` for private user feedback.
- Pass entity arguments to FTL so grammar functions can work.

## Examine

- Push localized markup.
- Avoid leaking hidden information.

## Entity Names

Use localization/prototype naming patterns instead of setting raw UI text in code.
