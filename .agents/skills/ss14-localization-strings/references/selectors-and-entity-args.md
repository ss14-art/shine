# Selectors And Entity Args

Use Fluent variables and entity helpers for grammar:

- `THE($ent)`
- `INDEFINITE($ent)`
- `SUBJECT($ent)`
- `OBJECT($ent)`
- `POSS-ADJ($ent)`
- `CONJUGATE-BE($ent)`

Pass entities or values from C# instead of prebuilding English fragments.
