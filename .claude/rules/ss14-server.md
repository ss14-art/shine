# SS14 Server

For `Content.Server/**`, use:

- `ss14-client-server-shared`;
- `ss14-netcode`;
- `ss14-databases-migrations` when persistence is involved.

Server code is authoritative. Validate client requests and keep new Shine server code under `Content.Server/_sh`.
