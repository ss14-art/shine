# SS14 Shared

For `Content.Shared/**`, use:

- `ss14-client-server-shared`;
- `ss14-prediction`;
- `ss14-netcode`;
- `ss14-ecs-components`;
- `ss14-ecs-systems`.

Shared code cannot depend on Client or Server code. New Shine shared code belongs under `Content.Shared/_sh`.
