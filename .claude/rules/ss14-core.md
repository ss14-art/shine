# SS14 Core

Read `.agents/rules` and route with `.agents/rules/ss14-skill-preflight-and-refresh.md`.

Hard rules:

- no `RobustToolbox/**` edits;
- new Shine code under `_sh`;
- mark non-`_sh` edits;
- localize player-facing text;
- check prediction and server validation.
