# Content.IntegrationTests Agent Rules

Use `ss14-testing`, `ss14-networking-prediction`, and domain skills.

- Use integration tests for networking, prediction, map/prototype loading, and multi-assembly behavior.
- Validate SQLite/Postgres paths when database behavior changes.
- Keep tests deterministic and avoid leaking state across runs.
